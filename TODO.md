# TODO

## Make site responsive for all mobile sizes
- [ ] Update `style.css` media queries to target existing classes in `index.html` (`.header`, `.nav`, `.theme-switch`, `.home`, `.about`, etc.)
- [ ] Add mobile overrides to remove fixed offsets (`margin-left: 800px`, `margin-left: -1000px`, etc.)
- [ ] Ensure hero section uses fluid layout (centered image/text; no overflow)
- [ ] Ensure header/nav wraps or stacks on small screens
- [ ] Verify contact/projects/cards still look good on mobile
- [x] Mark completed after CSS changes



